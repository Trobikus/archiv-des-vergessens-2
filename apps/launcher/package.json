{
  "name": "@adv/launcher",
  "version": "2.0.0",
  "private": true,
  "description": "Portable Siegel-Portal launcher for Archiv des Vergessens",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "cargo build --manifest-path src-tauri/Cargo.toml --release",
    "clippy": "cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
