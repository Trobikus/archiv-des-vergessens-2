# Do not invent (quick list)

If you cannot find it in-repo, do **not** introduce it as a new parallel system:

- zod / yup / valibot / io-ts
- Redux, Zustand, MobX, Signals libraries
- htm / React DOM APIs
- Tauri rusqlite / Rust game_loop for saves or combat
- Server-side Clan idle/raid simulation
- Offline fake chat/friends/guild success paths
- New npm workspace packages without explicit request
- Alternate save formats beside the envelope
- Purple/glow “AI default” redesign of the hub
- Old triangle SVG as Grimoire studio logo
- Feature branches (`cursor/*`, `feature/*`) unless user asks
- Force-push / hard-reset “cleanup”
- Editing `balancing.golden.json` only to silence a failing gate
- Server importing `@adv/sim` or `@adv/content`
